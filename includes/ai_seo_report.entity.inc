<?php

/**
 * @file
 * Entity class and controller for AI SEO Report.
 */

/**
 * The class used for AI SEO Report entities.
 */
class AiSEOReport extends Entity {

  /**
   * The report ID.
   *
   * @var int
   */
  public $report_id;

  /**
   * The entity type.
   *
   * @var string
   */
  public $entity_type = 'node';

  /**
   * The entity ID.
   *
   * @var int
   */
  public $entity_id;

  /**
   * The report type.
   *
   * @var string
   */
  public $report_type = 'seo_analysis';

  /**
   * The content title.
   *
   * @var string
   */
  public $title = '';

  /**
   * The focus keyword.
   *
   * @var string
   */
  public $focus_keyword;

  /**
   * The SEO score.
   *
   * @var int
   */
  public $score = 0;

  /**
   * The status (GREEN, AMBER, RED).
   *
   * @var string
   */
  public $status = 'RED';

  /**
   * The report data.
   *
   * @var array
   */
  public $report_data = [];

  /**
   * The HTML snapshot.
   *
   * @var string
   */
  public $html_snapshot;

  /**
   * The user ID.
   *
   * @var int
   */
  public $uid;

  /**
   * The created timestamp.
   *
   * @var int
   */
  public $created;

  /**
   * The changed timestamp.
   *
   * @var int
   */
  public $changed;

  /**
   * Constructs a new AiSEOReport object.
   */
  public function __construct(array $values = []) {
    parent::__construct($values, 'ai_seo_report');
  }

  /**
   * Implements EntityInterface::id().
   */
  public function id() {
    return isset($this->report_id) ? $this->report_id : NULL;
  }

  /**
   * Implements EntityInterface::entityType().
   */
  public function entityType() {
    return 'ai_seo_report';
  }

  /**
   * Implements EntityInterface::uri().
   */
  public function uri() {
    return [
      'path' => 'node/' . $this->entity_id . '/seo/' . $this->report_id,
    ];
  }

  /**
   * Overrides Entity::save().
   */
  public function save() {
    global $user;

    if (empty($this->uid)) {
      $this->uid = $user->uid;
    }

    $this->changed = REQUEST_TIME;

    if (empty($this->created)) {
      $this->created = REQUEST_TIME;
    }

    return parent::save();
  }

  /**
   * Returns the report URL (deprecated, use uri()].
   */
  public function url() {
    return 'node/' . $this->entity_id . '/seo/' . $this->report_id;
  }

  /**
   * Returns the report label.
   */
  public function label() {
    return t('SEO Report #@id for @title', [
      '@id' => $this->report_id,
      '@title' => $this->title,
    ]);
  }
}

/**
 * Controller class for AI SEO Report entities.
 */
class AiSEOReportController extends EntityPlusController {

  /**
   * Overrides EntityPlusController::create().
   */
  public function create(array $values = []) {
    $values += [
      'report_id' => NULL,
      'entity_type' => 'node',
      'entity_id' => 0,
      'report_type' => 'seo_analysis',
      'title' => '',
      'focus_keyword' => NULL,
      'score' => 0,
      'status' => 'RED',
      'report_data' => [],
      'html_snapshot' => NULL,
      'uid' => 0,
      'created' => REQUEST_TIME,
      'changed' => REQUEST_TIME,
    ];

    return parent::create($values);
  }

  /**
   * Overrides EntityPlusController::buildContent().
   */
  public function buildContent(EntityInterface $entity, $view_mode = 'full', $langcode = NULL, $content = []) {
    $build = parent::buildContent($entity, $view_mode, $langcode, $content);

    // Render the report.
    $build['report'] = ai_seo_advisor_render_results($entity->report_data);
    $build['report']['#weight'] = 0;

    // Add metadata.
    $build['metadata'] = [
      '#type' => 'container',
      '#attributes' => ['class' => ['ai-seo-report-metadata']],
      '#weight' => -10,
    ];

    $build['metadata']['created'] = [
      '#type' => 'item',
      '#title' => t('Created'),
      '#markup' => format_date($entity->created, 'medium'),
    ];

    $build['metadata']['user'] = [
      '#type' => 'item',
      '#title' => t('Generated by'),
      '#markup' => theme('username', ['account' => user_load($entity->uid)]),
    ];

    if (!empty($entity->focus_keyword)) {
      $build['metadata']['keyword'] = [
        '#type' => 'item',
        '#title' => t('Focus keyword'),
        '#markup' => check_plain($entity->focus_keyword),
      ];
    }

    return $build;
  }
}
